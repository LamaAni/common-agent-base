# Secrets — copy this file to .env and fill in values
# Never commit .env

# AI platform API key (if required by your setup)
PLATFORM_API_KEY=

# Slack bot token (optional — only needed for Slack integration)
SLACK_BOT_TOKEN=

# Add keys here as integrations are added
